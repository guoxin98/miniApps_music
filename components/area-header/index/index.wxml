<view class="header">
  <view class="title">{{title}}</view>
  <view class="right">
    <view class="slot">
      <slot></slot>
    </view>
    <view class="default" wx:if="{{showDefault}}">
      <text>{{rightText}}</text>
      <slot name="icon">
        <image class="icon" src="/assets/images/icons/arrow-right.png"></image>
      </slot>
    </view>
  </view>
</view>