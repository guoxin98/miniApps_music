<!--components/hightlight-text-view/index.wxml-->
<view class="hightlight-text">
  <view wx:for="{{textArr}}" class="overflow-ellipsis" wx:key="index" style="color:{{item === keywords ? hightLightColor: normalColor }}">
    {{item}}
  </view>
</view>